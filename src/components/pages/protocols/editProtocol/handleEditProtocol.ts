export const handleEditProtocol = async (data: any) => {
  console.log("edit protocol", data);

  const {
    branch,
    tradeUnionName,
    workplaceName,
    disputeStartDate,
    disputeReason,
  } = data;
};
